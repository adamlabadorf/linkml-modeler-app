{
  "name": "@linkml-editor/electron",
  "version": "0.1.0",
  "type": "commonjs",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "echo 'No electron tests yet' && exit 0"
  },
  "dependencies": {
    "@linkml-editor/core": "workspace:*"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "typescript": "^5.4.0"
  }
}
